# 生产环境环境变量配置
# 复制此文件为 .env.production 并填写实际值

# MongoDB 连接信息
MONGODB_URI=mongodb://<username>:<password>@mongo:27017/kanban?authSource=admin

# MongoDB 根用户信息（用于 MongoDB 容器初始化）
MONGO_INITDB_ROOT_USERNAME=<root_username>
MONGO_INITDB_ROOT_PASSWORD=<root_password>
